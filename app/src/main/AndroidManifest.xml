<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shares.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RetrievalPwd"
            android:label="找回密码"></activity>
        <activity android:name=".Register"
            android:label="注册账户"></activity>
        <activity android:name=".OperationCore"
            android:label="操作中心"></activity>
        <activity android:name=".MyweeklyPay"
            android:label="我的周薪"></activity>
        <activity android:name=".MyOrder"
            android:label="我的订单"></activity>
        <activity android:name=".MyAssets"
            android:label="我的资产"></activity>
        <activity android:name=".OperatAssets"
            android:label="资产管理"></activity>

    </application>

</manifest>